# Project: adams
# Date: 2014-01-31 09:00:36
# User: fracpete
#
adams.flow.control.Flow
	-annotation
	Uses a previously trained clusterer model (\'simplekmeans\') provided by the webservice\nto generate predictions (cluster and cluster memberships) for the incoming dataset.\nAlso removes the class attribute from the dataset.\nSee \"adams-weka-webservice-train-clusterer.flow\" flow for training (and storing) the clusterer model on the server-side.
	-actor
		adams.flow.source.SelectFile
			-stop-if-canceled
			true
			-custom-stop-message
			User canceled dataset selection!
			-file-chooser-title
			Select dataset to cross-validate
			-initial-dir
			${EXAMPLE_FLOWS}/data
			-extension
			ARFF
			-extension
			CSV
			-initial-file
			${EXAMPLE_FLOWS}/data/anneal_test.arff
	-actor
		adams.flow.transformer.WekaFileReader
			-loader
				weka.core.converters.ArffLoader
	-actor
		adams.flow.transformer.WekaFilter
			-name
			remove class attribute
			-filter
				weka.filters.unsupervised.attribute.Remove
					-R
					last
					
	-actor
		adams.flow.transformer.WekaWSPredictClusterer
			-model-name
			simplekmeans
	-actor
		adams.flow.transformer.WSTransformer
			-logging-level
			INFO
			-client
				adams.flow.webservice.PredictClusterer
	-actor
		adams.flow.transformer.Convert
			-conversion
				adams.data.conversion.DatasetToWekaInstances
	-actor
		adams.flow.sink.DisplayPanelManager
			-provider
				adams.flow.sink.WekaInstancesDisplay
					-writer
						adams.gui.print.NullWriter
	-flow-execution-listener
		adams.flow.execution.NullListener

