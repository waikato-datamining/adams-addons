# Project: adams
# Date: 2014-01-31 09:00:49
# User: fracpete
# Charset: UTF-8
#
adams.flow.control.Flow
	-annotation
	Trains a SimpleKMeans clusterer on a dataset that the user chooses through the referenced \nwebservice and stores the model there as well. Removes the class attribute from the data.\nSee flow defined in \"adams-weka-webservice-weka-webservice.flow\" for running the webservice.
	-actor
		adams.flow.standalone.CallableActors
			-actor
				adams.flow.source.WekaClustererSetup
					-clusterer
						weka.clusterers.SimpleKMeans
							-N
							2
							-A
							weka.core.EuclideanDistance -R first-last
							-I
							500
							-num-slots
							1
							-S
							10
	-actor
		adams.flow.source.SelectFile
			-stop-if-canceled
			true
			-custom-stop-message
			User canceled dataset selection!
			-file-chooser-title
			Select dataset to cross-validate
			-initial-dir
			${EXAMPLE_FLOWS}/data
			-extension
			ARFF
			-extension
			CSV
			-initial-file
			${EXAMPLE_FLOWS}/data/anneal_train.arff
	-actor
		adams.flow.transformer.WekaFileReader
			-loader
				weka.core.converters.ArffLoader
	-actor
		adams.flow.transformer.WekaFilter
			-name
			Remove class attribute
			-filter
				weka.filters.unsupervised.attribute.Remove
					-R
					last
					
	-actor
		adams.flow.transformer.WekaWSTrainClusterer
			-model-name
			simplekmeans
	-actor
		adams.flow.transformer.WSTransformer
			-logging-level
			INFO
			-client
				adams.flow.webservice.TrainClusterer
	-actor
		adams.flow.sink.Display
	-flow-execution-listener
		adams.flow.execution.NullListener

