# Configuration file listing the superclasses and the packages in which to
# look for derived classes.
#
# Format:
# class.name=package1,package2,...
#
# Author:  FracPete (fracpete at waikato dot ac dot nz)

# the REST plugins
adams.flow.dropwizard.RESTPlugin=\
  adams.flow.dropwizard,\
  adams.flow.dropwizard.echo

# the REST service providers
adams.flow.dropwizard.RESTProvider=\
  adams.flow.dropwizard,\
  adams.flow.dropwizard.echo

# the health checks
com.codahale.metrics.health.HealthCheck=\
  adams.flow.dropwizard.healthcheck
