# Project: adams
# Date: 2019-11-25 11:01:34
# User: fracpete
# Charset: UTF-8
# Modules: adams-core,adams-event,adams-groovy,adams-groovy-rest,adams-json,adams-net,adams-rest,adams-security,adams-webservice-core
#
adams.flow.control.Flow -annotation "Offers a simple \\\"echo\\\" REST service, which just sends back the text encoded in the URL.\\nThe REST service itself is implemented in the \\\"adams-groovy-rest_echo.groovy\\\" script." -flow-execution-listener adams.flow.execution.NullListener -flow-restart-manager adams.flow.control.flowrestart.NullManager
 adams.flow.standalone.RESTServer -web-service "adams.flow.rest.GroovyServer -logging-level INFO -in-interceptor adams.flow.rest.interceptor.incoming.NullGenerator -out-interceptor adams.flow.rest.interceptor.outgoing.NullGenerator -plugin ${FLOWS}/adams-groovy-rest_echo.groovy"
 adams.flow.source.Idle